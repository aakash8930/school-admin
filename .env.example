# Base URL for the NestJS API.
# In dev, leave as "/api" — Vite proxies it to http://localhost:3000 (see vite.config.ts).
# In production, set the full origin, e.g. https://api.yourschool.com/api
VITE_API_BASE_URL=/api
